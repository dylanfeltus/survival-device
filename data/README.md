# Data Directory

Place offline source documents here by default: `data/sources/`.

Supported source formats for v1:
- `.txt`
- `.md`
- `.markdown`

For richer documents (PDF/HTML), preconvert to UTF-8 text before ingestion.
